name: app #Environment name
dependencies:
  - python=3.7.10
  - pip:
    - dash
    - dash-renderer
    - dash-core-components
    - dash-html-components
    - plotly
    - gunicorn # for app deployment